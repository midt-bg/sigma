// Loaded by the `dom` Vitest project (see vitest.config.ts) for every `*.test.tsx`, so component tests
// get jest-dom matchers (toBeInTheDocument, …) without importing them per file. Lives under `app/` so
// the matcher type augmentation is also visible to `tsc`.
import '@testing-library/jest-dom/vitest';
