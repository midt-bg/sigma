// Types for the shared corpus key samples, so the Worker-side test can import the same list.
export declare const ACCEPTED_KEYS: string[];
export declare const REJECTED_KEYS: string[];
export declare const REJECTED_TRAVERSAL_KEYS: string[];
